class Flight {
    constructor(date, takeOffTime, landingTime, origin, destination) {
        this.date = date
        this.takeOffTime = takeOffTime
        this.landingTime = landingTime
        this.origin = origin
        this.destination = destination
    }
}

export default Flight